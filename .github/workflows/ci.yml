name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-type:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Create venv & install (core + dev)
        run: |
          uv venv
          uv pip install -e ".[dev]"

      - name: Ruff
        run: .venv/bin/ruff check .

      - name: mypy
        run: .venv/bin/mypy src

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Create venv & install (core + dev)
        run: |
          uv venv
          uv pip install -e ".[dev]"

      - name: Pytest
        run: .venv/bin/pytest -v

  smoke-cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Create venv & install (core only)
        run: |
          uv venv
          uv pip install -e .

      - name: drip --help
        run: .venv/bin/drip --help

      - name: drip run (offline samples, no API keys, no provider SDKs)
        run: .venv/bin/drip run
